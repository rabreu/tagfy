const PATH = ".//music_test"
const EXTENTIONS = [".mp3", ".flac"]
const PORT = 8080
const DATABASE_HOSTNAME = "localhost"
const DATABASE_PORT = 27017
const DATABASE_NAME = "tagfy"

module.exports = { 
    PATH,
    EXTENTIONS,
    PORT,
    DATABASE_HOSTNAME,
    DATABASE_PORT,
    DATABASE_NAME
}